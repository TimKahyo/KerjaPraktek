// nextjs group routes
// the name of the folder is callable as part of the route

const TestPage = () => {
    return (
    <div>
        Test Page
    </div>
    );
};
 
// put the export here not at the top because it wouldn't register as a route
export default TestPage;